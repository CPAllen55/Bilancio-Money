package com.bilanciomoney.bilancio.ui.theme

import androidx.compose.ui.graphics.Color

/* The website's palette (public/index.html), so the three apps read as one.
   Two golds, as on iOS: the deep one reads as gold on paper and goes muddy on
   black, the pale one glows on black and is illegible on white. */
val Gold = Color(0xFFA07026)
val GoldPale = Color(0xFFEFBD5A)
val Navy = Color(0xFF12253F)
val Paper = Color(0xFFF7F2E6)
val Night = Color(0xFF0E1726)
val NightCard = Color(0xFF17233A)

/* The last twentieth of a budget: not over yet, and close enough to say so.
   Amber rather than a pale red, because "nearly" is not "past". */
val Caution = Color(0xFFD98A1F)

/* Money in and money out, the same two the web app uses for its marks. */
val Positive = Color(0xFF1F9D63)
val Negative = Color(0xFFD64550)
